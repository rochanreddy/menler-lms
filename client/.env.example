# Base URL of the LMS backend API.
# Local: http://localhost:4100/api/lms
# Prod (lms.menler.in): https://<your-render-service>.onrender.com/api/lms
# Inlined at build time by Vite — changing it requires a redeploy.
VITE_API_URL=http://localhost:4100/api/lms
